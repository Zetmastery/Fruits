import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  controlsContainer: {
    position: 'absolute',
    top: 35,
    width: '100%',
    height: 40,
    paddingBottom: 15,
  },

  back: {
    alignSelf: 'flex-end',
    marginRight: 15,
  },
});

export default styles;
